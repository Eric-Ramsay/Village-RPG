#pragma once

enum LOGIN {
	CHOOSE,
	REGISTER_USERNAME,
	REGISTER_PASSWORD,
	LOGIN_USERNAME,
	LOGIN_PASSWORD,
	COMPLETED
};

enum ALIGN {
	LEFT,
	CENTER,
	RIGHT
};

enum SYMBOL {
	SQUARE,
	HEART,
	AP,
	DEFENSE,
	STAMINA,
	SP,
	LOCK,
	ARMOR,
	BUFF,
	DEBUFF
};